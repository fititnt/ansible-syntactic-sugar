---
# FILE:            {{ role_path }}/vars/main.yml
# LICENSE:         Public Domain
# DESCRIPTION:     This file {{ role_path }}/vars/main.yml always is loaded.

albextras_version: "v0.2.0-alpha"

albextras_internal_isconteiner: "{{ albextras_isconteiner | default(false) }}"

# Draft. Eventually could be useful. Not is for now (fititnt, 2020-01-03 03:55 BRT)
albextras_internal_rclone_manange: "{{ (albextras_rclone is defined) and (albextras_rclone|bool is sameas true) }}"
albextras_internal_devel_nginx_manange: "{{ (albextras_devel_nginx is defined) and (albextras_devel_nginx|bool is sameas true) }}"
